# Server-only target for the /api rewrite used by `next dev`.
CURTIS_BACKEND_URL=http://localhost:3001

# Enables the browser-only role picker and deterministic Faker data.
NEXT_PUBLIC_DEMO_MODE=false
