# Local backend configuration. NEVER commit .env.
# scripts/env.sh copies this file to .env when .env is missing.

# Entra ID app client secret (in app registration "Curtis").
# Without the real value the server boots, but Microsoft login will fail.
CLIENT_SECRET=
